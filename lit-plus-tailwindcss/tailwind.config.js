module.exports = {
    mode: 'jit',
    purge: ['./src/**/*.{html,ts,js}'],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
}